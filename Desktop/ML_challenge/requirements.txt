# Core Python Dependencies (Platform-Agnostic)
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.2.0
rapidfuzz>=3.0.0
xgboost>=2.0.0
lightgbm>=4.0.0
tqdm>=4.65.0
pytest>=7.0.0

# Optional GPU acceleration for NVIDIA RTX 4500 Ada / CUDA:
# Install PyTorch with CUDA support matching your CUDA driver, e.g.:
# pip install torch --index-url https://download.pytorch.org/whl/cu121
